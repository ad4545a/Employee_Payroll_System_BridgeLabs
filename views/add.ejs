<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Employee - Payroll System</title>
    <link rel="stylesheet" href="/styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
</head>

<body>
    <div class="container" style="max-width: 600px;">
        <header style="text-align: center; border-bottom: none; margin-bottom: 1rem;">
            <div>
                <h1>EPS</h1>
                <p>Add New Talent</p>
            </div>
        </header>

        <div class="card">
            <h2 style="text-align: center; margin-bottom: 2rem;">Add Employee</h2>

            <form action="/add" method="POST">
                <div class="form-group">
                    <label for="name" class="form-label">Full Name</label>
                    <input type="text" id="name" name="name" class="form-input" placeholder="e.g. Sarah Connor"
                        required>
                </div>

                <div class="form-group">
                    <label for="position" class="form-label">Department</label>
                    <input type="text" id="position" name="position" class="form-input" placeholder="e.g. Engineering"
                        required>
                </div>

                <div class="form-group">
                    <label for="basicSalary" class="form-label">Basic Salary (₹)</label>
                    <input type="number" id="basicSalary" name="basicSalary" class="form-input" placeholder="e.g. 50000"
                        min="0" step="0.01" required>
                    <small class="form-hint">Tax (12%) and Net Salary are calculated automatically.</small>
                </div>

                <div class="form-actions">
                    <a href="/" class="btn btn-secondary">Cancel</a>
                    <button type="submit" class="btn btn-primary">Add Employee</button>
                </div>
            </form>
        </div>
    </div>
</body>

</html>