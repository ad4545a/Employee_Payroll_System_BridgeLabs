<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Employee - Payroll System</title>
    <link rel="stylesheet" href="/styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
</head>

<body>
    <div class="container">
        <header class="header">
            <h1 class="title">
                <span class="icon">✏️</span>
                Edit Employee
            </h1>
            <a href="/" class="btn btn-secondary">
                ← Back to Dashboard
            </a>
        </header>

        <div class="card">
            <form action="/edit/<%= employee.id %>" method="POST" class="form">
                <div class="form-group">
                    <label for="name" class="form-label">Full Name</label>
                    <input type="text" id="name" name="name" class="form-input" placeholder="Enter employee name"
                        value="<%= employee.name %>" required>
                </div>

                <div class="form-group">
                    <label for="position" class="form-label">Position</label>
                    <input type="text" id="position" name="position" class="form-input" placeholder="Enter job position"
                        value="<%= employee.position %>" required>
                </div>

                <div class="form-group">
                    <label for="basicSalary" class="form-label">Basic Salary (₹)</label>
                    <input type="number" id="basicSalary" name="basicSalary" class="form-input"
                        placeholder="Enter basic salary" value="<%= employee.basicSalary %>" min="1" step="0.01"
                        required>
                    <small class="form-hint">Tax (12%) and net salary will be calculated automatically</small>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn btn-primary btn-large">
                        Update Employee
                    </button>
                    <a href="/" class="btn btn-secondary btn-large">
                        Cancel
                    </a>
                </div>
            </form>
        </div>
    </div>
</body>

</html>